#!/bin/sh

# setting up env
NOWM_CONFIG_HOME="${XDG_CONFIG_HOME:=${HOME}/.config}/nowm"
export NOWM_CONFIG_HOME

# load ~/nowm/nowmrc
[ -x "${NOWM_CONFIG_HOME}/nowmrc" ] && "${NOWM_CONFIG_HOME}/nowmrc" 2>&1 >/dev/null &

# main loop (do nothing)
while : ; do : ; done
